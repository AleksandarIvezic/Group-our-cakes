
<%= form_with model: @cake do |form| %>
  <% if @cake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cake.errors.count, "error") %> prohibited this @cake from being saved:</h2>

      <ul>
        <% @cake.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field author_id: current_user.id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>